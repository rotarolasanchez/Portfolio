package presentation.view.atoms.theme

import androidx.compose.ui.graphics.Color

// ── Colores de marca Capibara ──────────────────────────────────────────────
val CapibaraBrownLight  = Color(0xFFD2B48C) // Pelaje claro  → primaryContainer light
val CapibaraBrownMedium = Color(0xFF8B4513) // Pelaje medio  → primary light
val CapibaraBrownDark   = Color(0xFF5C4033) // Pelaje oscuro → onPrimaryContainer, onBackground

val CapibaraBeigeLight  = Color(0xFFFEE7D7) // Beige claro   → background, secondaryContainer
val CapibaraBeigeMedium = Color(0xFFC0A080) // Beige medio
val CapibaraBeigeDark   = Color(0xFF8C6D56) // Beige oscuro  → secondary, outline

val CapibaraGreyLight   = Color(0xFFCFC8B6) // Gris cálido claro
val CapibaraGreyMedium  = Color(0xFFA89F94) // Gris cálido medio
val CapibaraGreyDark    = Color(0xFF6F675E) // Gris cálido oscuro → tertiary

val CapibaraGreigeLight  = Color(0xFFD8D2C1) // Greige claro  → surfaceVariant, tertiaryContainer
val CapibaraGreigeMedium = Color(0xFFB0A799) // Greige medio  → outline dark
val CapibaraGreigeDark   = Color(0xFF7C7465) // Greige oscuro → onSurfaceVariant

// ── Éxito / Error ──────────────────────────────────────────────────────────
val GreenSuccess80 = Color(0xFFA5D6A7) // Verde claro (on dark backgrounds)
val GreenSuccess40 = Color(0xFF388E3C) // Verde oscuro  (on light backgrounds)

val RedError80 = Color(0xFFEF9A9A)     // Rojo claro → errorContainer light
val RedError40 = Color(0xFFD32F2F)     // Rojo oscuro → error light / onErrorContainer dark

