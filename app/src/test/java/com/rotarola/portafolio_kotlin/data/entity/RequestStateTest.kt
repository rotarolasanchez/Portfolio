package com.rotarola.portafolio_kotlin.data.entity

import com.rotarola.portafolio_kotlin.domain.model.RequestState
import junit.framework.TestCase.assertEquals
import org.junit.Test
import org.junit.runner.RunWith
import org.junit.runners.JUnit4
/*
@RunWith(JUnit4::class)
class RequestStateTest {
    @Test
    fun `when RequestState is Success then data is set correctly`() {
        val data = listOf("data")
        val state = RequestState.Success(data)
        assertEquals(state.data, data)
    }

    @Test
    fun `when RequestState is Error then exception is set correctly`() {
        val exception = Exception("error")
        val state = RequestState.Error(exception)
        assertEquals(state.error, exception)
    }
}*/